import { HignlightedDirective } from './hignlighted.directive';

describe('HignlightedDirective', () => {
  it('should create an instance', () => {
    const directive = new HignlightedDirective();
    expect(directive).toBeTruthy();
  });
});
